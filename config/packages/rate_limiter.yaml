framework:
    rate_limiter:
        register_api:
            policy: 'sliding_window'
            limit: 3
            interval: '1 hour'
        login_api:
            policy: 'sliding_window'
            limit: 10
            interval: '1 hour'

when@test:
    framework:
        rate_limiter:
            login_api:
                policy: 'sliding_window'
                limit: 1000
                interval: '1 hour'

